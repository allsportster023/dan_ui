import { createTheme } from '@mui/material/styles';

export default createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#f9a01b'
        },
        secondary: {
            main: '#118eab'
        }
    },
    typography: {
        htmlFontSize: 13,
        fontSize: 12
    }
});
